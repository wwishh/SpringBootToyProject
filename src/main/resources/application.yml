server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/wish?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: rhqemddl12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false  # 개발 중에는 캐시를 비활성화해 변경 사항이 바로 반영되도록 합니다.

logging:
  level:
    org.hibernate.SQL: debug


